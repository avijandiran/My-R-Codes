cars
head(cars)

cars.distance <- dist(cars)

# small values indicates dissimilarity and large values indicates similarity 
#2 and 1 is 8.000 (similarity)  3 and 1 is 3.60 (dissimilarity)

cars.distance  # symentric matrix

